# Generated by `ash discover --write`
agents:
  opencode:
    type: local-cli
    cmd: opencode
    args: ["run"]
    model_flag: --model
    session_flag: --continue
    stdin_prompt: false
  claude-code:
    type: local-cli
    cmd: claude
    model_flag: --model
    session_flag: --continue
    message_flag: --msg
    stdin_prompt: false
