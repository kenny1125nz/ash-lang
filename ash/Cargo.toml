[package]
name = "ash"
version = "0.1.4"
edition = "2021"

[dependencies]
regex = "1"
log = { version = "0.4", features = ["std"] }
rustyline = { version = "14", optional = true }

[features]
default = ["repl"]
repl = ["rustyline"]
