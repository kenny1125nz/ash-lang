# Generated by ash
agents:
  aider:
    type: local-cli
    driver: aider
    cmd: aider
    model_flag: --model
    session_flag: --restore-chat-history
    message_flag: --msg
    stdin_prompt: false
    yes_flag: --yes
  opencode:
    type: local-cli
    driver: opencode
    cmd: opencode
    args:
      - run
    model_flag: --model
    session_flag: --continue
    stdin_prompt: false
