# Unify documentation across website, GitHub README, npm README, and VS Code README

after publish ash to npm / vs code extension/ github / web-site,  a key issue has surfaced that synching the features ash has becomes a tedious task.

we want ensure consistent information across all publishes, yet each has it's own focus that
- **npm**  focus on encurge user start using it with minimum learning needs or coganitive loads
- **github** focus on providing full spec on what and how ash can be used, organized in a managed learning curve
- **web-site** focus on what problem ash solves, the pain point of AI augemented work, and provide full spec of all ash's announced features
- **vscode extension** assume user has already installed ash and being using it, it shold focus on what's the extra value the extension could bring in, syntax highlight and compiling check (good for feedback loop for coding agent)

Another big pain point I was facing was the contents were generated by AI, and full of misleading information

So we need a solution that 
- centralized documentations/units of ash features/specs/examples/
- expected parts are assembled into target publishes based on their needs.

one big challenge of the issue is the web-site,  truth could be tracked as documents as markdown, we need work-out a mechnism to render markdown in html web-site while keeping the web-site's style