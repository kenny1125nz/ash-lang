# NPM Package

Package ash for distribution via npm:
- Configure package.json (bin entry, dependencies, metadata)
- Set up build pipeline for publishing
- Verify global install works: `npm i -g ash` → `ash` available on PATH
